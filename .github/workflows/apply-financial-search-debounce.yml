name: Apply Financial Search Debounce

on:
  push:
    branches: [owner-source]
    paths:
      - 'scripts/apply_financial_search_debounce.py'
      - '.github/workflows/apply-financial-search-debounce.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout owner-source
        uses: actions/checkout@v4
        with:
          ref: owner-source
          fetch-depth: 0

      - name: Preserve patch helper
        run: cp scripts/apply_financial_search_debounce.py /tmp/apply_financial_search_debounce.py

      - name: Patch Owner
        run: python scripts/apply_financial_search_debounce.py

      - name: Commit Owner
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add lib/screens/shared/user_profile_screen.dart scripts/verify_online_only.py
          if ! git diff --cached --quiet; then
            git commit -m 'Debounce Financial Chat full-history search hydration'
            git push origin HEAD:owner-source
          fi

      - name: Checkout user-source
        run: |
          git fetch origin user-source
          git checkout -B user-source origin/user-source
          cp /tmp/apply_financial_search_debounce.py /tmp/patch.py

      - name: Patch User
        run: python /tmp/patch.py

      - name: Commit User
        run: |
          git add lib/screens/shared/user_profile_screen.dart scripts/verify_online_only.py
          if ! git diff --cached --quiet; then
            git commit -m 'Debounce Financial Chat full-history search hydration'
            git push origin HEAD:user-source
          fi
