name: Customer Profile Redesign Once
'on':
  push:
    branches:
      - owner-source
    paths:
      - .github/workflows/profile-tabs-redesign-once.yml

permissions:
  contents: write

jobs:
  patch-both:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: owner-source
          fetch-depth: 0
      - name: Patch Owner profile
        run: |
          python3 scripts/profile_tabs_patch.py
          git diff --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add lib/screens/shared/user_profile_screen.dart
          if ! git diff --cached --quiet; then
            git commit -m 'Split customer profile into focused sections'
            git push origin owner-source
          fi
          cp scripts/profile_tabs_patch.py /tmp/profile_tabs_patch.py
      - name: Patch User profile
        run: |
          git fetch origin user-source
          git checkout -B user-source origin/user-source
          python3 /tmp/profile_tabs_patch.py
          git diff --check
          git add lib/screens/shared/user_profile_screen.dart
          if ! git diff --cached --quiet; then
            git commit -m 'Split customer profile into focused sections'
            git push origin user-source
          fi
